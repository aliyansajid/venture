import SectionHeader from "@/components/SectionHeader";
import TopHeader from "@/components/TopHeader";

const Settings = () => {
  return (
    <section>
      <TopHeader />
      <SectionHeader title="Settings"></SectionHeader>
    </section>
  );
};

export default Settings;
