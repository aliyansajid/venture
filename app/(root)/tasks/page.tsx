import SectionHeader from "@/components/SectionHeader";
import TopHeader from "@/components/TopHeader";

const Tasks = () => {
  return (
    <section>
      <TopHeader />
      <SectionHeader title="Tasks"></SectionHeader>
    </section>
  );
};

export default Tasks;
