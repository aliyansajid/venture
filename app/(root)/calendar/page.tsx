import SectionHeader from "@/components/SectionHeader";
import TopHeader from "@/components/TopHeader";

const Calendar = () => {
  return (
    <section>
      <TopHeader />
      <SectionHeader title="Calendar"></SectionHeader>
    </section>
  );
};

export default Calendar;
