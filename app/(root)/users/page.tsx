import SectionHeader from "@/components/SectionHeader";
import TopHeader from "@/components/TopHeader";

const Users = () => {
  return (
    <section>
      <TopHeader />
      <SectionHeader title="Messages"></SectionHeader>
    </section>
  );
};

export default Users;
